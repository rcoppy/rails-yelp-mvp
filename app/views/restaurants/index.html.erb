<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
    <div class="restaurant">
        <h2><%= restaurant.name %></h2>
        <p><%= restaurant.category %></p>
        <p><%= restaurant.phone_number %></p>
        <p><%= restaurant.address %></p>

        <%= link_to 'add a review', new_restaurant_review(restaurant) %>
    </div>
<% end %>